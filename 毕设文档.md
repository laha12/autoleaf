## OpenCV

### 形态学知识

![image-20260228211753322](C:\Users\86185\AppData\Roaming\Typora\typora-user-images\image-20260228211753322.png)

#### 腐蚀

腐蚀的基本思想就像土壤侵蚀一样，它会腐蚀前景对象的边界（始终尝试将前景保持为白色）。那么它是如何工作的呢？内核在图像上滑动（如在 2D 卷积中）。原始图像中的像素（无论是 1 还是 0）只有当内核下的所有像素都为 1 时才被视为 1，否则它会被腐蚀（变为零）。

因此发生的情况是，所有靠近边界的像素都会根据内核的大小被丢弃。因此，前景对象的厚度或大小减小，或者图像中的白色区域简单地减小。它可用于去除小的白色噪点（正如我们在颜色空间章节中看到的那样），分离两个连接的对象等。

在这里，作为一个例子，我将使用一个 5x5 的内核，其中充满了 1。让我们看看它是如何工作的

```python
import cv2 as cv
import numpy as np
 
img = cv.imread('j.png', cv.IMREAD_GRAYSCALE)
assert img is not None, "无法读取文件，请检查 os.path.exists()"
kernel = np.ones((5,5),np.uint8)
erosion = cv.erode(img,kernel,iterations = 1)
```

![image-20260228211801130](C:\Users\86185\AppData\Roaming\Typora\typora-user-images\image-20260228211801130.png)

#### 膨胀

它与腐蚀正好相反。在这里，如果内核下至少有一个像素是“1”，则像素元素为“1”。因此，它增加了图像中的白色区域或前景对象的大小。通常，在诸如去除噪声的情况下，腐蚀之后是膨胀。因为，腐蚀会去除白色噪声，但它也会缩小我们的对象。因此我们膨胀它。由于噪声已经消失，它们不会再回来，但我们的对象区域会增加。它也有助于连接对象的断裂部分。

```python
dilation = cv.dilate(img,kernel,iterations = 1)
```

![image-20260228211953173](C:\Users\86185\AppData\Roaming\Typora\typora-user-images\image-20260228211953173.png)

#### 开运算

开运算只是**先腐蚀后膨胀**的另一个名称。如上所述，它有助于去除噪声。

```python
opening = cv.morphologyEx(img, cv.MORPH_OPEN, kernel)
```

![image-20260228211939394](C:\Users\86185\AppData\Roaming\Typora\typora-user-images\image-20260228211939394.png)

#### 闭运算

闭运算是开运算的逆运算，**先膨胀后腐蚀**。它有助于闭合前景对象内部的小孔，或者对象上的小黑点。

```
closing = cv.morphologyEx(img, cv.MORPH_CLOSE, kernel)
```

![image-20260228211932539](C:\Users\86185\AppData\Roaming\Typora\typora-user-images\image-20260228211932539.png)

### 图像平滑-滤波

[OpenCV: 图像平滑 - OpenCV 计算机视觉库](https://docs.opencv.ac.cn/4.12.0/d4/d13/tutorial_py_filtering.html)

#### 1. 高斯噪声的本质（通俗版）

高斯噪声是图像中最常见的一种**随机噪声**，可以理解为：

> 图像的每个像素值被叠加了一个 “随机数”，这个随机数的取值服从**正态分布（高斯分布）**。

举个直观的例子：

- 原本像素值为 200 的绿色叶片像素，叠加高斯噪声后，可能变成 198、203、195 等，数值波动的概率符合 “中间值（0 偏移）概率最高，偏移越大概率越低” 的规律。

#### 2. 数学原理（精准版）

- 高斯分布（正态分布）的概率密度函数：

  ![image-20260228213159389](C:\Users\86185\AppData\Roaming\Typora\typora-user-images\image-20260228213159389.png)

  - μ（均值）：噪声的平均偏移量（通常为 0，即噪声无偏）；
  - σ（标准差）：噪声的强度（σ越大，噪声越明显）。

  

- 图像中受高斯噪声污染的像素值：

- ![image-20260228213151927](C:\Users\86185\AppData\Roaming\Typora\typora-user-images\image-20260228213151927.png)

   是服从高斯分布的随机数。

#### 3. 高斯噪声的视觉特征

- 表现：图像整体呈现 “颗粒感”“雾蒙蒙” 的效果，没有固定的形状（区别于椒盐噪声的黑白点）；

- 特点：噪声分布在所有像素上，强度随机但符合正态分布（大部分像素噪声偏移小，少数偏移大）；

- 示例对比：

  | 原始图像                       | 加高斯噪声的图像                                     |
  | :----------------------------- | :--------------------------------------------------- |
  | 清晰的叶片纹理，像素值平滑过渡 | 叶片纹理边缘模糊，整体有细密的颗粒感，像素值随机波动 |

#### 4. 高斯噪声的产生原因（工程场景）

在你的叶片图像处理场景中，噪声主要来自：

1. **成像设备**：相机传感器的电子热噪声、感光元件的暗电流；
2. **传输过程**：图像从相机传输到电脑时的信号干扰；
3. **光照条件**：弱光环境下，相机自动提高 ISO，导致噪声放大；
4. **图像压缩 / 存储**：JPG 等压缩格式的有损压缩引入的伪噪声。

#### 5. 为什么必须处理高斯噪声（结合你的代码）

```
blurred = cv2.GaussianBlur(gray, (5, 5), 0)
```

这一步就是专门处理高斯噪声，原因是：

- **边缘检测对噪声极度敏感**：Canny 边缘检测的核心是计算像素梯度（像素值的变化率），高斯噪声会导致像素值随机波动，产生大量 “伪边缘”（比如原本平滑的叶片区域，噪声会让梯度值突变，被误判为边缘）；
- **不处理的后果**：边缘掩码会充满杂乱的伪边缘，轮廓提取时无法找到完整的叶片轮廓，最终 ROI 提取失败。

### Canny边缘检测

![image-20260228214651226](C:\Users\86185\AppData\Roaming\Typora\typora-user-images\image-20260228214651226.png)

![image-20260228214602053](C:\Users\86185\AppData\Roaming\Typora\typora-user-images\image-20260228214602053.png)

x为右-左，y为下-上

![image-20260228214750564](C:\Users\86185\AppData\Roaming\Typora\typora-user-images\image-20260228214750564.png)

## 卷积神经网络

简单来说：卷积用来提取特征，池化压缩特征，全连接层用来加权重

### 卷积层

![在这里插入图片描述](https://i-blog.csdnimg.cn/blog_migrate/d2c4e2d16f97dcb091cd996da9b2d362.gif#pic_center)

黄色的区域表示卷积核在输入矩阵中滑动, 每滑动到一个位置, 将对应数字相乘并求和, 得到一个特征图矩阵的元素. 注意到, **动图中卷积核每次滑动了一个单位**, 实际上滑动的幅度可以根据需要进行调整. **如果滑动步幅大于 1**, 则卷积核有**可能无法恰好滑到边缘**, 针对这种情况, 可在矩阵最外层补零, 补一层零后的矩阵如下图所示：

![在这里插入图片描述](https://i-blog.csdnimg.cn/blog_migrate/99b36bd6d50e1ef0c7763182482c821a.png)

可根据需要设定补零的层数. 补零层称为 Zero Padding, 是一个可以设置的超参数, 但要根据卷积核的大小, 步幅, 输入矩阵的大小进行调整, 以使得卷积核恰好滑动到边缘

![image-20260302155652774](C:\Users\86185\AppData\Roaming\Typora\typora-user-images\image-20260302155652774.png)

上图是对一个特征图采用一个卷积核卷积的过程, 为了提取更多的特征, 可以采用多个卷积核分别进行卷积, 这样便可以得到多个特征图. 有时, 对于一张三通道彩色图片, 或者如第三层特征图所示, 输入的是一组矩阵, 这时卷积核也不再是一层的, 而要变成相应的深度.



![在这里插入图片描述](https://i-blog.csdnimg.cn/blog_migrate/6b85560a887d753b2affc8f38157669f.gif)

上图中, 最左边是输入的特征图矩阵, 深度为 3, 补零(Zero Padding)层数为 1, 每次滑动的步幅为 2. 中间两列粉色的矩阵分别是两组卷积核, 一组有三个, 三个矩阵分别对应着卷积左侧三个输入矩阵, 每一次滑动卷积会得到三个数, 这三个数的和作为卷积的输出. 最右侧两个绿色的矩阵分别是两组卷积核得到的特征图.

### 池化层

![image-20260302160122997](C:\Users\86185\AppData\Roaming\Typora\typora-user-images\image-20260302160122997.png)



### CABM（卷积块注意力机制）

![image-20230313201848619](https://i-blog.csdnimg.cn/blog_migrate/89eece7f213bb567b7d2320cc02a34e2.png#pic_center)

首先针对一个原始的特征图，即输入特征图Input Feature。接着会将输入特征图送入一个Channel Attention Module[通道注意力模块]和Spatial Attention Module[空间注意力模块]，最终会的到一个Refined Feature。

#### Channel Attention Module

![img](https://i-blog.csdnimg.cn/blog_migrate/a427dc55797de0b7b7d074b1cb2f80fa.png#pic_center)

首先我们会对输入特征图F分别做一个全局最大池化下采样和全局平均池化下采样，F由原来的H × W × C 变成两个1 × 1 × C 的特征图，接着我们会将这两个特征图送入到两个全连接层中，最终会输出两个1 × 1 × C  的特征图。得到两个1 × 1 × C 的特征图后，我们将其相加并经过sigmoid激活函数将其值限制在0-1之前，这就得到了最后的Channel Attention，即上图中的M c，其尺寸为1 × 1 × C 。这个过程用公式表示如下：

![image-20230313204812628](https://i-blog.csdnimg.cn/blog_migrate/fc8f8488218dbba9fb7cfba8d68f5c6a.png#pic_center)



#### Spatial Attention Module

但是大家这里需要注意一点，我们在通道注意力中得到了1 × 1 × C 1 \times 1 \times C1×1×C尺寸的特征图M c，我们并不是将M c 作为输入送到空间注意力中，而是先用F和M c 相乘，得到特征图F ′ ，F ′ 的尺寸和F一致，都是H × W × C 

![image-20230313205632671](https://i-blog.csdnimg.cn/blog_migrate/d85a443e8d56e70a003c2c8448f14768.png#pic_center)

首先我们知道在上一步我们得到了一个H × W × C 大小的特征图F ′ ，空间注意力首先同样会分别进行一个**全局最大池化下采样**和**全局平均池化下采样**，但是这时候我们是在**channel维度**上做，全局最大池化下采用会得到上图蓝色的特征图，其尺寸为H × W × 1 ，全局平均池化下采样会得到上图橙色的特征图，其尺寸为H × W × 1 。然后我们将橙色和蓝色的特征图在channel维度进行拼接，会到底H × W × 2 大小的特征图。接着会做一次卷积，将刚刚得到的H × W × 2的特征图变成H × W × 1 的特征图。最后同样接一个sigmoid激活函数将特征图的值限制在0-1之前，即得到最终的M s ，其尺寸为H × W × 1 

![image-20230313211307154](https://i-blog.csdnimg.cn/blog_migrate/7f220b7df1f6fa44bb37796f8cf09d22.png#pic_center)



## Grad_CAM

CNN（卷积神经网络），在图像识别中，表现卓越，但是模型的“黑盒”特性让人难以信服他的每个预测结果，所以就需要一个“可解释性人工智能”来使得模型具有一定的可解释性，如Grad-CAM。



### 基本概念

Grad-CAM 是一种基于梯度的类激活映射方法，它可以生成一张[热力图](https://zhida.zhihu.com/search?content_id=259739518&content_type=Article&match_order=1&q=热力图&zhida_source=entity)，突出显示图像中对模型预测贡献最大的区域。

![img](https://pic2.zhimg.com/v2-7878303b040543ac04d332a56cf9d0db_r.jpg)

它的核心思路是：通过分析模型最后一个卷积层的[梯度信息](https://zhida.zhihu.com/search?content_id=259739518&content_type=Article&match_order=1&q=梯度信息&zhida_source=entity)，找出哪些图像区域对当前预测类别最关键。与早期需要修改模型结构的方法不同，Grad-CAM直接作用于预训练模型，简单、高效。

### 如何工作





## 数据集

LeafSanp 以及

plantvillage[PlantVillage Dataset](https://www.kaggle.com/datasets/emmarex/plantdisease?resource=download)